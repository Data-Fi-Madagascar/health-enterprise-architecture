name: CI — Validation HEA

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Validate referentiel graph
        run: python3 scripts/validate_ref.py

      - name: Check referentiel index idempotence
        run: python3 scripts/build_ref_index.py --check

      - name: Check wrappers idempotence
        run: python3 scripts/build_wrappers.py --check

      - name: Check broken links
        run: python3 scripts/check_links.py

      - name: Check documentation manifests
        run: python3 scripts/check_manifests.py

      - name: Compile RDF and validate SHACL shapes
        run: python3 scripts/compile_rdf.py --validate

      - name: Check JSON Schema idempotence
        run: python3 scripts/compilers/compile_jsonschema.py --check

      - name: Compile FHIR R4 (DO → StructureDefinition, CodeSystem, ValueSet)
        run: python3 scripts/compilers/compile_fhir.py --validate

      - name: Check OpenAPI idempotence
        run: python3 scripts/compilers/compile_openapi.py --check

      - name: Check ODA idempotence
        run: python3 scripts/compilers/compile_oda.py --check

      - name: Validate governance (fichiers d'auteur)
        run: python3 scripts/compilers/compile_oda.py --check-governance

      - name: Check Mintlify artifact idempotence
        run: python3 scripts/build_mintlify.py --check

      - name: Upload RDF artifact
        uses: actions/upload-artifact@v4
        with:
          name: hea-rdf
          path: dist/hea.ttl
          retention-days: 30

      - name: Upload ODA artifacts
        uses: actions/upload-artifact@v4
        with:
          name: hea-oda
          path: |
            dist/fhir/
            03_ptisn/schemas/
          retention-days: 30
